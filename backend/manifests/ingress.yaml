apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: todo-backend-ing
spec:
  rules:
  - http:
      paths:
      - path: /api
        backend:
          serviceName: todo-backend-svc
          servicePort: 2345